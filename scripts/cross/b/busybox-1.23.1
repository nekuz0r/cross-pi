#!/bin/bash

PKG_NAME=busybox-1.23.1
PKG_URL=http://www.busybox.net/downloads/${PKG_NAME}.tar.bz2

pkg_configure() {
	make defconfig
}

pkg_build() {
	make
}

pkg_install() {
	make install CONFIG_PREFIX=${CROSSPI_TARGET_DIR}
}
