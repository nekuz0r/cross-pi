#!/bin/bash

PKG_NAME=renderproto-0.11.1
PKG_URL=http://xorg.freedesktop.org/releases/individual/proto/${PKG_NAME}.tar.bz2

pkg_configure() {
	${PKG_SRC_DIR}/configure 	--prefix=/usr \
					--build=${CROSSPI_BUILD} \
					--host=${CROSSPI_TARGET} \
					--target=${CROSSPI_TARGET}
}

pkg_install() {
	make install
}
