#!/bin/bash

PKG_NAME=libffi-3.2.1
PKG_SRC_URL=ftp://sourceware.org/pub/libffi/${PKG_NAME}.tar.gz

crosspi_configure() {
	$PKG_SRC_DIR/configure --prefix=$CROSSPI_HOST_DIR/usr
}

crosspi_build() {
	make
}

crosspi_install() {
	make install
}
